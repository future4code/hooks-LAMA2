export type Band = {
    id: string;
    responsible: string;
    name: string;
    musicGenre: string;
}

export interface BandDTO {
    name: string;
    responsible:string; 
    musicGenre:string;
}